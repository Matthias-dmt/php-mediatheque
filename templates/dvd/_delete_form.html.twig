<form method="post" action="{{ path('d_v_d_delete', {'id': d_v_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ d_v_d.id) }}">
    <button class="btn">Delete</button>
</form>
