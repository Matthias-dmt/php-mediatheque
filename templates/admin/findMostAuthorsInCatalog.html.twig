<section class="card m-3 p-0" style="width: 18rem;">
    <div class="card-header p-3">
        <h5 class="card-title">Auteurs les plus présents dans le catalogue</h5>
    </div>
    <ul class="list-group list-group-flush">
    {% for author in authors %}
        <li class="list-group-item">{{author.lastName}} {{author.firstName}} - {{author.c}} fois</li>
    {% endfor %}
    </ul>
</section>

