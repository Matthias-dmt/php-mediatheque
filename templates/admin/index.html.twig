{% extends 'base.html.twig' %}

{% block title %}Admin
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Admin</h1>
        <a href="{{ path('author_index') }}">Auteur</a><br>
        <a href="{{ path('document_index') }}">Document</a><br>
        <a href="{{ path('book_index') }}">Livre</a><br>
        <a href="{{ path('e_book_index') }}">Livre Numérique</a><br>
        <a href="{{ path('journal_index') }}">Journal</a><br>
        <a href="{{ path('dvd_index') }}">DVD</a><br>
        <a href="{{ path('cd_index') }}">CD</a>
	</div>
{% endblock %}
