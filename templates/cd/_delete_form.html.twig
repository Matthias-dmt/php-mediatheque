<form method="post" action="{{ path('c_d_delete', {'id': c_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_d.id) }}">
    <button class="btn">Delete</button>
</form>
